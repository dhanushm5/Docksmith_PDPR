# Docksmith_PDPR

Project layout:

- `docksmith/`: original working directory (mixed history, includes mac helper script)
- `docksmith-linux/`: Linux-focused no-Docker variant

Use `docksmith-linux/` on Linux to avoid macOS/Linux workflow confusion.
